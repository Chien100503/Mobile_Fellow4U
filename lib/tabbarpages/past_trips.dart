import 'package:flutter/material.dart';

class PastTrip extends StatelessWidget {
  const PastTrip({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Past Trips')),
    );
  }
}
