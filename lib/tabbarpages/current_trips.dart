import 'package:flutter/material.dart';

class CurrentTrip extends StatelessWidget {
  const CurrentTrip({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Current Trips')),
    );
  }
}
