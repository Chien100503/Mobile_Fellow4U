import 'package:flutter/material.dart';

class NextTrip extends StatelessWidget {
  const NextTrip({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Next Trips')),
    );
  }
}
