import 'package:flutter/material.dart';

class WishTrip extends StatelessWidget {
  const WishTrip({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Wish Trips')),
    );
  }
}
